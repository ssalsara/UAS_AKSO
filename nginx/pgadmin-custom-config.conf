# Konfigurasi pgAdmin 4

# Mengatur mode debug
DEBUG = False

# Mengatur pengaturan server
SERVER_NAME = 'My pgAdmin Server'
SERVER_PORT = 80

# Mengatur pengaturan email
MAIL_SERVER = 'smtp.example.com'
MAIL_PORT = 587
MAIL_USE_TLS = True
MAIL_USERNAME = 'your-email@example.com'
MAIL_PASSWORD = 'your-email-password'

# Mengatur pengaturan database
SQLALCHEMY_DATABASE_URI = 'postgresql://admin:admin@postgres_service:5432/exampledb'

# Mengatur pengaturan lainnya
ITEMS_PER_PAGE = 25